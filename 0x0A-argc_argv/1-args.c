#include <stdio.h>

/**
 * main - Entry point
 * @argc: number of argument passed
 * @agrv: argument passed into it
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d/n", argc);

	return (0);
}
